exports.testIndexValidations = [
    {
        param_key: 'username',
        required: true,
        type: 'string',
        // validator_functions: [(param) => {
        //     return param.length === 10
        // }]
    },

    {
        param_key: 'password',
        required: true,
        type: 'string',
        // validator_functions: [(param) => {
        //     return param.length === 10
        // }]
    },

]
